# Milestones Local

A responsive, local-first replica of the Milestones project planning interface.

## Run

```bash
npm install
npm run dev
```

Project data is stored in the browser with `localStorage`. No account or iCloud sync is used.
